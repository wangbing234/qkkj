<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="transArea">	<select id="transArea.selectPageList" parameterType="net.onlineshop.services.common.TransArea" resultType="net.onlineshop.services.common.TransArea">	 select * 	 from t_trans_area t where 1=1  order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="transArea.selectPageCount" parameterType="net.onlineshop.services.common.TransArea" resultType="java.lang.Integer">	 select count(*)   from t_trans_area t where 1=1 	</select>		<select id="transArea.selectList" parameterType="net.onlineshop.services.common.TransArea" resultType="net.onlineshop.services.common.TransArea">		 		 	 <if test="parent_id==null or parent_id==''">	  select * from t_trans_area obj  where obj.parent_id is null order by obj.sequence asc	 </if>	 	 <if test="parent_id!=null and parent_id!=''">	 	 	 	  select * from t_trans_area obj  where obj.parent_id =#{parent_id}  order by obj.sequence asc	     </if>	</select>		 	<insert id="transArea.insert" parameterType="net.onlineshop.services.common.TransArea" useGeneratedKeys="true" keyProperty="id">		insert into t_trans_area		(		id				)		values		(		id			)</insert>	<update id="transArea.update" parameterType="net.onlineshop.services.common.TransArea">		update t_trans_area		  set id=#{id}				 where 1=1 and id=#{id}	</update>	<select id="transArea.selectOne" parameterType="net.onlineshop.services.common.TransArea" resultType="net.onlineshop.services.common.TransArea">	 select * from t_trans_area t where 1=1 and id=#{id}	</select>	<delete id="transArea.deleteById" parameterType="net.onlineshop.services.common.TransArea">		delete from t_trans_area where 1=1 and id=#{id}	</delete>		<delete id="transArea.deleteAll">		delete from t_trans_area where 1=1 	</delete>		<delete id="transArea.delete" parameterType="net.onlineshop.services.common.TransArea">		delete from t_trans_area where code=#{code}	</delete></mapper>